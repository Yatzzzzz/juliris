# Database Connection

> [!CAUTION]
> Credentials have been moved to `.env.local` for security.
> Do NOT commit credentials to version control.
>
> See `.env.local` for DB_HOST, DB_USER, DB_PASSWORD, DB_NAME, DB_PORT.
